# КОМПЛЕКСНЫЙ ПЛАН ИСПРАВЛЕНИЯ — AIGirlsHub

> Глубокий анализ и пошаговый план по превращению полурабочего сайта в консистентный, логичный продукт

**Дата анализа:** 2025-12-11  
**Статус:** Требует комплексной переработки

---

## КРАТКОЕ РЕЗЮМЕ

### 🔴 Критические проблемы (исправить немедленно):
1. **Фильтры в `/catalog`** — кнопки без функционала, хотя компонент `CatalogFilters` существует
2. **Битая ссылка в `Hero.astro`** — `/top/best-free` не существует
3. **Нет логотипов моделей** — директория `/public/images/models/` отсутствует
4. **Лишние страницы** — `/ids-docs`, `/story` не должны быть на проде

### 🟠 Архитектурные проблемы:
1. Дублирование компонентов (`ModelCard` vs `ModelCardSemantic`)
2. Неиспользуемые компоненты (`Hero`, `TopListItem`, `QuickPick`, `CategoryCards`)
3. Нет страницы 404
4. Дублирование навигации (4 способа попасть в каталог)

### 🟡 UX проблемы:
1. Главная слишком минималистична — нет превью сервисов
2. Нет "быстрого старта" для нерешительных
3. FAQ свёрнут — SEO-контент скрыт
4. Нет индикации свежести контента

### ✅ Что работает хорошо:
1. Страницы целей `/for/[goal]` с таблицами сравнения
2. Страницы моделей `/models/[slug]` со структурой "убеждения"
3. Фильтрация через `/catalog/[tag]` (но не на главной `/catalog`)
4. Сравнения `/compare/[slug]`
5. Дизайн-токены в `tokens.css`
6. Header с dropdown и мобильным меню

---

## ОГЛАВЛЕНИЕ

1. [Диагностика: Текущее состояние](#1-диагностика-текущее-состояние)
2. [Критические баги (блокеры)](#2-критические-баги-блокеры)
3. [Архитектурные проблемы](#3-архитектурные-проблемы)
4. [Проблемы консистентности дизайна](#4-проблемы-консистентности-дизайна)
5. [UX/UI проблемы](#5-uxui-проблемы)
6. [Проблемы контента и данных](#6-проблемы-контента-и-данных)
7. [SEO и технические проблемы](#7-seo-и-технические-проблемы)
8. [План исправлений по фазам](#8-план-исправлений-по-фазам)
9. [Чеклист для проверки](#9-чеклист-для-проверки)

---

## 1. ДИАГНОСТИКА: ТЕКУЩЕЕ СОСТОЯНИЕ

### 1.1 Что есть сейчас

```
СТРУКТУРА САЙТА:
├── / (главная)                    — Выбор цели + FAQ
├── /catalog                       — Все сервисы с фильтрами
├── /for/[goal]                    — Страницы по целям (5 шт)
├── /models/[slug]                 — Страницы моделей (10 шт)
├── /compare                       — Индекс сравнений
├── /compare/[slug]                — Страницы сравнений (3 шт)
├── /about, /privacy, /disclaimer  — Служебные
└── /ids-docs                      — Документация IDS (лишняя)
```

### 1.2 Что работает ✅

- **Базовая структура** — Astro, Content Collections, компоненты
- **Дизайн-токены** — `tokens.css` с переменными
- **Смысловая сегментация** — 4 уровня в схеме данных
- **Страницы целей** — `/for/[goal]` с таблицами сравнения
- **Страницы моделей** — структура "убеждения"
- **Сравнения** — работают корректно
- **Header** — dropdown для целей, мобильное меню
- **SEO базовый** — meta-теги, Schema.org

### 1.3 Что НЕ работает ❌

- **Фильтры в каталоге** — кнопки есть, но фильтрация не реализована
- **Страницы топов** — удалены, но ссылки могут остаться
- **Страницы тегов** — `/tag/[tag]` существует в коде, но не используется
- **Поиск** — отсутствует
- **Мобильная адаптация** — частично сломана

---

## 2. КРИТИЧЕСКИЕ БАГИ (БЛОКЕРЫ)

### 🔴 BUG-001: Фильтры в каталоге — несогласованность реализации

**Где:** `/catalog` → кнопки фильтров  
**Проблема:** На странице `/catalog` используются `<button>` без функционала, хотя существует компонент `CatalogFilters.astro` с рабочими ссылками на `/catalog/[tag]`.

**Файл:** `src/pages/catalog.astro` — использует кнопки без JS  
**Файл:** `src/pages/catalog/[tag].astro` — использует `CatalogFilters` с ссылками ✅

**Решение:**
1. Заменить кнопки в `catalog.astro` на компонент `CatalogFilters`
2. Удалить дублирующийся код фильтров
3. Использовать единый компонент везде

---

### ✅ РЕШЕНО: Страницы тегов реализованы как `/catalog/[tag]`

**Где:** `src/pages/catalog/[tag].astro`  
**Статус:** Работает корректно!

Страницы фильтрации существуют:
- `/catalog/free` — бесплатные
- `/catalog/russian` — на русском
- `/catalog/nsfw` — 18+
- `/catalog/voice` — с голосом
- `/catalog/image-gen` — генерация фото

**Проблема:** Главная страница `/catalog` не использует компонент `CatalogFilters`.

---

### 🔴 BUG-003: Битая ссылка на /top/best-free в Hero.astro

**Где:** `src/components/Hero.astro` строка 32  
**Проблема:** Компонент содержит ссылку на несуществующую страницу `/top/best-free`.

```astro
<CTAButton href="/top/best-free" text="Лучшие бесплатные" external={false} variant="secondary" size="L" />
```

**Решение:**
1. Заменить `/top/best-free` на `/catalog/free` или `/for/flirt`
2. Или удалить эту кнопку
3. Проверить, используется ли `Hero.astro` где-либо (сейчас не используется на главной)

---

### 🔴 BUG-004: Лишняя страница /ids-docs

**Где:** `src/pages/ids-docs.astro`  
**Проблема:** Документация дизайн-системы не должна быть на продакшн-сайте.

**Решение:**
1. Удалить `src/pages/ids-docs.astro`
2. Или переместить в отдельную директорию с условным рендерингом

---

### 🔴 BUG-005: Страница /story — непонятное назначение

**Где:** `src/pages/story.astro`  
**Проблема:** Не описана в архитектуре, непонятно зачем нужна.

**Решение:**
1. Проверить содержимое
2. Удалить если не нужна
3. Или интегрировать в `/about`

---

## 3. АРХИТЕКТУРНЫЕ ПРОБЛЕМЫ

### 🟠 ARCH-001: Дублирование навигации

**Проблема:** Пользователь может попасть к сервисам через:
1. Header → "Выбрать AI" dropdown → `/for/[goal]`
2. Header → "Все сервисы" → `/catalog`
3. Главная → карточки целей → `/for/[goal]`
4. Главная → "Смотреть все N сервисов" → `/catalog`

**Решение:**
- Определить ОДИН основной путь
- Главная = выбор цели (decision tree)
- Каталог = полный список для тех, кто знает что ищет
- Убрать дублирование CTA

---

### 🟠 ARCH-002: Непоследовательная иерархия страниц

**Проблема:**
```
/ → /for/flirt → /models/candy-ai   ✅ Логично
/ → /catalog → /models/candy-ai     ✅ Логично
/ → /compare → /compare/candy-vs-character → ???  ❓ Куда дальше?
```

**Решение:**
- На странице сравнения добавить чёткие CTA к обеим моделям
- Добавить "Вернуться к выбору" → `/for/[goal]`

---

### 🟠 ARCH-003: Отсутствие единой точки входа для "топов"

**Проблема:** Топы удалены, но пользователи могут искать "лучшие AI-девушки".

**Решение:**
- Страницы `/for/[goal]` уже выполняют эту функцию
- Добавить SEO-оптимизированные заголовки: "Лучшие AI для флирта 2025"
- Или создать отдельные лендинги `/best/free`, `/best/nsfw`

---

### 🟠 ARCH-004: Неиспользуемые компоненты

**Проблема:** В `/src/components/` есть компоненты, которые могут не использоваться:
- `ModelCard.astro` vs `ModelCardSemantic.astro` — дублирование?
- `TopListItem.astro` — топы удалены
- `VerdictBox.astro` — используется?
- `QuickPick.astro` — используется?
- `CategoryCards.astro` — используется?

**Решение:**
1. Провести аудит использования каждого компонента
2. Удалить неиспользуемые
3. Объединить дублирующиеся

---

## 4. ПРОБЛЕМЫ КОНСИСТЕНТНОСТИ ДИЗАЙНА

### 🟡 DESIGN-001: Смешение старых и новых компонентов

**Проблема:** Есть `ModelCard.astro` и `ModelCardSemantic.astro`. Какой использовать?

**Текущее использование:**
- `catalog.astro` → `ModelCardSemantic`
- `for/[goal].astro` → `ModelCardSemantic`

**Решение:**
1. Удалить `ModelCard.astro` если не используется
2. Переименовать `ModelCardSemantic.astro` → `ModelCard.astro`
3. Обновить все импорты

---

### 🟡 DESIGN-002: Эмодзи вместо иконок

**Проблема:** Везде используются эмодзи: 🔥 🎭 💬 🎤 📸

**Где:**
- `index.astro` — карточки целей
- `Header.astro` — dropdown
- `for/[goal].astro` — hero
- `ModelCardSemantic.astro` — иконка цели
- `FeatureBadge.astro` — бейджи фич

**Решение:**
1. Оставить как есть (эмодзи универсальны)
2. ИЛИ заменить на SVG-иконки (Lucide, Heroicons)
3. Создать компонент `Icon.astro` для единообразия

---

### 🟡 DESIGN-003: Разные стили карточек на разных страницах

**Проблема:**
- `/catalog` — карточки с CTA кнопками
- `/for/[goal]` — карточки с CTA кнопками
- `/compare` — карточки сравнений (другой стиль)

**Решение:**
- Унифицировать базовый стиль карточки через `Card.astro`
- Варианты: `Card`, `Card--interactive`, `Card--compare`

---

### 🟡 DESIGN-004: Непоследовательные отступы между секциями

**Проблема:** Используются разные комбинации `<Space size="L" />`, `<Space size="XL" />`.

**Решение:**
1. Определить стандарт: между секциями = `XL`, внутри секции = `L`
2. Применить везде единообразно

---

### 🟡 DESIGN-005: Разные стили заголовков секций

**Проблема:**
- Где-то `<h2>` с классом
- Где-то `<SectionTitle>`
- Где-то просто `<h2>`

**Решение:**
1. Использовать `<SectionTitle>` везде для заголовков секций
2. Или определить CSS-класс `.section-title` и применять везде

---

## 5. UX/UI ПРОБЛЕМЫ

### 🟡 UX-001: Главная страница слишком минималистична

**Проблема:** После редизайна главная показывает только:
- Заголовок "Что ты ищешь?"
- 5 карточек целей
- Ссылку на каталог
- FAQ

**Что потеряно:**
- Превью сервисов (пользователь не видит что есть)
- Социальное доказательство (сколько обзоров, рейтинги)
- "Выбор редакции" / Featured модели

**Решение:**
1. Добавить секцию "Топ-3 по рейтингу" под целями
2. Добавить статистику: "10 сервисов • 3 сравнения • Обновлено 2025"
3. Или оставить минимализм, но добавить визуальные якоря (логотипы сервисов)

---

### 🟡 UX-002: Нет "быстрого старта" для нерешительных

**Проблема:** Пользователь должен выбрать цель. А если не знает?

**Решение:**
1. Добавить блок "Не знаешь с чего начать? → Character.AI (бесплатно)"
2. Или добавить "Самый популярный" бейдж на одну из целей

---

### 🟡 UX-003: Страница модели — нет галереи/скриншотов

**Проблема:** Пользователь не видит интерфейс сервиса перед переходом.

**Решение:**
1. Добавить поле `screenshots: string[]` в схему
2. Создать компонент `Gallery.astro` (уже есть в IDS)
3. Добавить секцию "Скриншоты" на страницу модели

---

### 🟡 UX-004: Нет индикации "свежести" контента

**Проблема:** Пользователь не знает, актуальны ли обзоры.

**Решение:**
1. Добавить поле `updatedAt: Date` в схему моделей
2. Показывать "Обновлено: декабрь 2025" на карточках/страницах

---

### 🟡 UX-005: FAQ свёрнут — SEO-контент скрыт

**Проблема:** FAQ на главной свёрнут по умолчанию. Пользователи не видят ответы.

**Решение:**
1. Раскрыть первые 2 вопроса по умолчанию
2. Или показать краткие ответы, а полные — по клику

---

### 🟡 UX-006: Нет 404 страницы

**Проблема:** При переходе на несуществующую страницу — стандартная ошибка Astro.

**Решение:**
1. Создать `src/pages/404.astro`
2. Добавить полезные ссылки: Главная, Каталог, Популярные

---

### 🟡 UX-007: Нет страницы поиска / результатов

**Проблема:** Поиска нет вообще. При 10 моделях это терпимо, но при росте — критично.

**Решение (на будущее):**
1. Интегрировать Pagefind (статический поиск)
2. Или Fuse.js (клиентский fuzzy search)
3. Добавить поле поиска в header

---

## 6. ПРОБЛЕМЫ КОНТЕНТА И ДАННЫХ

### 🟡 DATA-001: Неполные данные в моделях

**Проблема:** Не все модели имеют заполненные поля:
- `tagline` — может отсутствовать
- `productFit.idealFor` — может быть пустым
- `productFit.alternatives` — может быть пустым
- `content` — Markdown-обзор отсутствует

**Решение:**
1. Провести аудит всех 10 YAML-файлов
2. Заполнить недостающие поля
3. Добавить валидацию в CI/CD

---

### � DATA-002: Директория с логотипами моделей отсутствует

**Проблема:** Директория `/public/images/models/` НЕ СУЩЕСТВУЕТ. Все логотипы отсутствуют!

**Текущая структура `/public/images/`:**
```
images/
├── favicon-inverted.png
├── gallery/       (пусто)
├── icons/         (пусто)
└── inline-gallery/ (пусто)
```

**Решение:**
1. Создать директорию `/public/images/models/`
2. Добавить логотипы для всех 10 моделей (webp формат)
3. Fallback в компонентах уже есть (показывает первую букву)

---

### 🟡 DATA-003: Сравнения — только 3 штуки

**Проблема:** Для 10 моделей возможно 45 комбинаций, но есть только 3 сравнения.

**Решение:**
1. Добавить ещё 5-7 популярных сравнений
2. Или создать автогенерацию сравнений на основе данных

---

### 🟡 DATA-004: Affiliate URL могут быть устаревшими

**Проблема:** Партнёрские ссылки могут перестать работать.

**Решение:**
1. Регулярно проверять ссылки
2. Добавить мониторинг (например, через GitHub Actions)

---

## 7. SEO И ТЕХНИЧЕСКИЕ ПРОБЛЕМЫ

### 🟡 SEO-001: Нет sitemap.xml

**Проблема:** Sitemap не генерируется автоматически.

**Решение:**
1. Установить `@astrojs/sitemap`
2. Настроить в `astro.config.mjs`

---

### 🟡 SEO-002: Нет robots.txt

**Проблема:** Файл robots.txt отсутствует.

**Решение:**
1. Создать `/public/robots.txt`
2. Разрешить индексацию, исключить служебные страницы

---

### 🟡 SEO-003: OG-картинки — одна на все страницы

**Проблема:** Используется `/images/og/default.jpg` везде.

**Решение:**
1. Создать уникальные OG-картинки для каждой модели
2. Или генерировать динамически (Satori, @vercel/og)

---

### 🟡 SEO-004: Canonical URL может быть неправильным

**Проблема:** `Astro.site` может быть не настроен.

**Решение:**
1. Проверить `astro.config.mjs` → `site`
2. Убедиться что canonical URL корректный

---

### 🟡 TECH-001: Нет обработки ошибок в getCollection

**Проблема:** Если данные некорректны, сайт может упасть при сборке.

**Решение:**
1. Добавить try/catch в критичных местах
2. Добавить валидацию данных перед сборкой

---

### 🟡 TECH-002: Нет TypeScript strict mode

**Проблема:** Могут быть скрытые ошибки типизации.

**Решение:**
1. Включить `strict: true` в `tsconfig.json`
2. Исправить все ошибки типов

---

## 8. ПЛАН ИСПРАВЛЕНИЙ ПО ФАЗАМ

### ФАЗА 0: КРИТИЧЕСКИЕ БАГИ (1 день)

| # | Задача | Файлы | Приоритет |
|---|--------|-------|-----------|
| 0.1 | Реализовать фильтрацию в каталоге | `catalog.astro` | 🔴 |
| 0.2 | Удалить/проверить страницы тегов | `pages/tag/` | 🔴 |
| 0.3 | Удалить страницу ids-docs | `pages/ids-docs.astro` | 🔴 |
| 0.4 | Проверить страницу story | `pages/story.astro` | 🔴 |
| 0.5 | Найти и исправить битые ссылки на /top/ | Все файлы | 🔴 |

---

### ФАЗА 1: АРХИТЕКТУРА (2-3 дня)

| # | Задача | Файлы | Приоритет |
|---|--------|-------|-----------|
| 1.1 | Аудит и удаление неиспользуемых компонентов | `components/` | 🟠 |
| 1.2 | Унифицировать ModelCard (удалить дубли) | `components/ModelCard*.astro` | 🟠 |
| 1.3 | Создать страницу 404 | `pages/404.astro` | 🟠 |
| 1.4 | Упростить навигацию (убрать дублирование) | `Header.astro`, `index.astro` | 🟠 |
| 1.5 | Проверить все внутренние ссылки | Все страницы | 🟠 |

---

### ФАЗА 2: ДИЗАЙН-КОНСИСТЕНТНОСТЬ (2-3 дня)

| # | Задача | Файлы | Приоритет |
|---|--------|-------|-----------|
| 2.1 | Унифицировать отступы между секциями | Все страницы | 🟡 |
| 2.2 | Унифицировать заголовки секций | Все страницы | 🟡 |
| 2.3 | Проверить мобильную адаптацию | Все компоненты | 🟡 |
| 2.4 | Унифицировать стили карточек | `Card.astro`, страницы | 🟡 |
| 2.5 | Решить вопрос с эмодзи/иконками | Компоненты | 🟡 |

---

### ФАЗА 3: UX УЛУЧШЕНИЯ (3-4 дня)

| # | Задача | Файлы | Приоритет |
|---|--------|-------|-----------|
| 3.1 | Добавить превью сервисов на главную | `index.astro` | 🟡 |
| 3.2 | Добавить "быстрый старт" для нерешительных | `index.astro` | 🟡 |
| 3.3 | Раскрыть первые FAQ по умолчанию | `index.astro` | 🟡 |
| 3.4 | Добавить галерею на страницу модели | `models/[slug].astro` | 🟡 |
| 3.5 | Добавить индикацию свежести контента | Карточки, страницы | 🟡 |

---

### ФАЗА 4: КОНТЕНТ И ДАННЫЕ (2-3 дня)

| # | Задача | Файлы | Приоритет |
|---|--------|-------|-----------|
| 4.1 | Аудит и заполнение данных моделей | `content/models/*.yaml` | 🟡 |
| 4.2 | Проверить наличие всех логотипов | `public/images/models/` | 🟡 |
| 4.3 | Добавить ещё 5 сравнений | `content/comparisons/` | 🟡 |
| 4.4 | Проверить affiliate URL | `content/models/*.yaml` | 🟡 |

---

### ФАЗА 5: SEO И ТЕХНИЧЕСКОЕ (1-2 дня)

| # | Задача | Файлы | Приоритет |
|---|--------|-------|-----------|
| 5.1 | Установить и настроить sitemap | `astro.config.mjs` | 🟡 |
| 5.2 | Создать robots.txt | `public/robots.txt` | 🟡 |
| 5.3 | Проверить canonical URL | `SiteLayout.astro` | 🟡 |
| 5.4 | Включить TypeScript strict | `tsconfig.json` | 🟢 |
| 5.5 | Создать уникальные OG-картинки | `public/images/og/` | 🟢 |

---

### ФАЗА 6: БУДУЩИЕ УЛУЧШЕНИЯ (опционально)

| # | Задача | Сложность | Приоритет |
|---|--------|-----------|-----------|
| 6.1 | Добавить поиск (Pagefind) | Высокая | 🟢 |
| 6.2 | Dark mode | Высокая | 🟢 |
| 6.3 | Автогенерация сравнений | Средняя | 🟢 |
| 6.4 | Динамические OG-картинки | Средняя | 🟢 |
| 6.5 | A/B тестирование CTA | Высокая | 🟢 |

---

## 9. ЧЕКЛИСТ ДЛЯ ПРОВЕРКИ

### После каждой фазы проверить:

#### Функциональность
- [ ] Все страницы открываются без ошибок
- [ ] Все внутренние ссылки работают
- [ ] Фильтры в каталоге работают
- [ ] Мобильное меню работает
- [ ] Dropdown в header работает

#### Консистентность
- [ ] Отступы между секциями одинаковые
- [ ] Заголовки секций в едином стиле
- [ ] Карточки выглядят одинаково везде
- [ ] Цвета соответствуют токенам

#### Мобильная версия
- [ ] Главная адаптирована
- [ ] Каталог адаптирован
- [ ] Страница модели адаптирована
- [ ] Header/Footer адаптированы

#### SEO
- [ ] Все страницы имеют уникальный title
- [ ] Все страницы имеют description
- [ ] Schema.org разметка корректна
- [ ] Canonical URL корректный

---

## ПРИЛОЖЕНИЕ A: КОНКРЕТНЫЕ ИСПРАВЛЕНИЯ

### A.1 Исправить фильтры в catalog.astro

**Файл:** `src/pages/catalog.astro`

**Заменить строки 49-63:**
```astro
<!-- БЫЛО: кнопки без функционала -->
<div class="filters">
  {filters.map(filter => (
    <button ...>
```

**На:**
```astro
<!-- СТАЛО: использовать готовый компонент -->
import CatalogFilters from '../components/CatalogFilters.astro';
...
<CatalogFilters activeFilter="all" totalCount={models.length} />
```

---

### A.2 Исправить Hero.astro

**Файл:** `src/components/Hero.astro` строка 32

**Заменить:**
```astro
<CTAButton href="/top/best-free" text="Лучшие бесплатные" ...
```

**На:**
```astro
<CTAButton href="/catalog/free" text="Лучшие бесплатные" ...
```

---

### A.3 Создать директорию для логотипов

```bash
mkdir -p public/images/models
```

Нужны файлы:
- `candy-ai.webp`
- `character-ai.webp`
- `replika.webp`
- `muah-ai.webp`
- `crushon-ai.webp`
- `dreamgf.webp`
- `kupid-ai.webp`
- `anima-ai.webp`
- `nastia-ai.webp`
- `chai-ai.webp`

---

## ПРИЛОЖЕНИЕ B: КОМАНДЫ ДЛЯ ПРОВЕРКИ

```bash
# Сборка проекта
npm run build

# Проверка TypeScript
npx tsc --noEmit

# Поиск битых ссылок на /top/
grep -r "/top/" src/

# Поиск использования компонентов
grep -r "Hero.astro" src/pages/
grep -r "ModelCard.astro" src/pages/
grep -r "TopListItem" src/pages/
grep -r "QuickPick" src/pages/

# Проверка наличия логотипов
ls -la public/images/models/ 2>/dev/null || echo "Директория не существует!"
```

---

## ПРИЛОЖЕНИЕ C: ФАЙЛЫ ДЛЯ УДАЛЕНИЯ

```
src/pages/ids-docs.astro          — Документация IDS (не для прода)
src/pages/story.astro             — Проверить, возможно удалить
src/components/ModelCard.astro    — Если дублирует ModelCardSemantic
src/components/TopListItem.astro  — Если топы удалены
src/components/QuickPick.astro    — Если не используется
src/components/CategoryCards.astro — Если не используется
```

---

## ПРИЛОЖЕНИЕ D: ФАЙЛЫ ДЛЯ СОЗДАНИЯ

```
src/pages/404.astro               — Страница ошибки
public/robots.txt                 — Для SEO
public/images/models/             — Директория с логотипами (КРИТИЧНО!)
public/images/og/[model].jpg      — OG-картинки для моделей
```

---

## ЗАКЛЮЧЕНИЕ

Сайт имеет **хорошую техническую основу** (Astro, Content Collections, дизайн-токены), но страдает от:

1. **Незавершённости** — фильтры не работают, топы удалены
2. **Непоследовательности** — разные стили, дублирование компонентов
3. **Минимализма в ущерб UX** — нет превью, нет быстрого старта

**Рекомендуемый порядок:**
1. Сначала исправить критические баги (Фаза 0)
2. Затем навести порядок в архитектуре (Фаза 1)
3. После этого улучшать UX и контент (Фазы 3-4)

**Оценка времени:** 10-15 дней при работе 4-6 часов в день.

---

## 10. ДОПОЛНИТЕЛЬНЫЕ КРИТИЧЕСКИЕ НАХОДКИ (Глубокий аудит)

> Добавлено после детального анализа user flow и UX

---

### 🔴 CRITICAL-001: Footer содержит битую ссылку на /top

**Где:** `src/components/Footer.astro` строка 15  
**Проблема:** Footer содержит ссылку `{ href: '/top', label: 'Топы' }`, но страницы `/top` не существует!

**Влияние:** Каждая страница сайта имеет битую ссылку в футере.

**Решение:**
1. Удалить ссылку на `/top` из Footer
2. Или заменить на `/for/flirt` (самая популярная цель)

---

### 🔴 CRITICAL-002: Страница /for/[goal] — перегруз информацией

**Где:** `src/pages/for/[goal].astro`  
**Проблема:** На одной странице слишком много секций:
1. Hero с иконкой и описанием
2. "Лучший выбор" (большой блок)
3. "Какой опыт ищешь?" (4 карточки типов опыта)
4. "Все сервисы" (полный грид карточек с CTA)
5. "Сравнение сервисов" (огромная таблица)
6. CTA "Не нашёл подходящий?"

**User Flow проблема:**
- Пользователь видит "Лучший выбор" → хочет кликнуть
- Но потом скроллит и видит ещё 5+ карточек с CTA
- Затем таблицу сравнения
- **Cognitive overload** — слишком много выборов

**Решение:**
1. Убрать секцию "Какой опыт ищешь?" — она не кликабельна и не фильтрует
2. Ограничить грид карточек до 3-4 (топовых), остальные — ссылка "Показать все"
3. Таблицу сравнения сделать collapsible или вынести на отдельную страницу

---

### 🔴 CRITICAL-003: Карточки с двойным CTA — путаница

**Где:** `ModelCardSemantic.astro` с `showCTA={true}`  
**Проблема:** Карточка имеет:
- Клик по карточке → `/models/[slug]`
- Кнопка "Подробнее" → `/models/[slug]` (дубль!)
- Кнопка "Попробовать →" → affiliate URL

**User Flow проблема:**
- Две разные точки входа на одну страницу (карточка + кнопка)
- Пользователь не понимает разницу между кликом по карточке и "Подробнее"

**Решение:**
1. Убрать кнопку "Подробнее" — клик по карточке уже ведёт туда
2. Оставить только "Попробовать →" как единственный CTA
3. Или сделать карточку некликабельной, если есть CTA кнопки

---

### 🔴 CRITICAL-004: Таблица сравнения на /for/[goal] — UX катастрофа

**Где:** `FeatureComparisonTable.astro` на страницах целей  
**Проблема:**
- Таблица показывает ВСЕ модели для цели (до 8+)
- На мобильных — горизонтальный скролл
- 10 строк фич × 8 колонок = 80 ячеек данных
- Пользователь не может сравнить больше 3 моделей одновременно

**Решение:**
1. Ограничить таблицу до топ-3 моделей
2. Добавить ссылку "Сравнить все" → `/compare`
3. На мобильных показывать карточки вместо таблицы

---

### 🟠 UX-008: Секция "Какой опыт ищешь?" — мёртвый UI

**Где:** `src/pages/for/[goal].astro` строки 155-168  
**Проблема:** Карточки типов опыта ("Романтика", "Откровенно", "GFE") — это просто текст. Они:
- Не кликабельны
- Не фильтруют модели
- Не ведут никуда

**Влияние:** Пользователь ожидает интерактивность, но её нет.

**Решение:**
1. **Вариант A:** Удалить секцию полностью
2. **Вариант B:** Сделать карточки фильтрами (но это новая механика)
3. **Вариант C:** Добавить к каждой карточке рекомендуемую модель

---

### 🟠 UX-009: Главная страница — нет социального доказательства

**Где:** `src/pages/index.astro`  
**Проблема:** Пользователь видит:
- "Что ты ищешь?" (вопрос)
- 5 карточек целей с числами ("5", "7", "6"...)
- Ссылку на каталог
- FAQ

**Что отсутствует:**
- Логотипы/названия реальных сервисов (Candy.AI, Replika...)
- Рейтинги или отзывы
- "Выбор редакции" или "Популярное"
- Любое визуальное подтверждение качества контента

**Влияние:** Пользователь не понимает, что за сервисы его ждут.

**Решение:**
1. Добавить под целями секцию "Популярные сервисы" с 3 логотипами
2. Или показать мини-превью топ-3 моделей
3. Или добавить строку "Candy.AI • Replika • Character.AI и ещё 7"

---

### 🟠 UX-010: Страница модели — нет навигации "назад к цели"

**Где:** `src/pages/models/[slug].astro`  
**Проблема:** Breadcrumbs: Главная → Каталог → [Модель]

Но если пользователь пришёл с `/for/flirt`, он теряет контекст.

**Решение:**
1. Добавить в breadcrumbs цель, если она известна (через referrer или query param)
2. Или добавить блок "Другие сервисы для [цели]" внизу страницы

---

### 🟠 UX-011: Страница сравнений /compare — скучный список

**Где:** `src/pages/compare/index.astro`  
**Проблема:** Показывает только 3 сравнения в виде карточек. Нет:
- Фильтрации по моделям
- Поиска
- Рекомендаций "Популярные сравнения"

**Влияние:** При 3 сравнениях — терпимо. При 10+ — неудобно.

**Решение (на будущее):**
1. Добавить фильтр по модели
2. Или автогенерацию сравнений

---

### 🟠 UX-012: Компонент QuickPick не используется

**Где:** `src/components/QuickPick.astro`  
**Проблема:** Отличный компонент "Не знаешь с чего начать?" с 3 рекомендациями:
- Бесплатно → Character.AI
- Генерация фото → Candy.AI
- На русском → Replika

**Но он нигде не используется!**

**Решение:**
1. Добавить на главную страницу после карточек целей
2. Или добавить на `/catalog` для нерешительных

---

### 🟠 UX-013: CategoryCards ссылается на несуществующий /tag/[slug]

**Где:** `src/components/CategoryCards.astro` строка 24  
**Проблема:** Компонент генерирует ссылки `/tag/${cat.slug}`, но такой роут не существует!

**Решение:**
1. Исправить на `/catalog/${cat.slug}`
2. Или удалить компонент, если не используется

---

### 🟡 DESIGN-006: Неконсистентные CSS переменные

**Проблема:** В разных компонентах используются разные форматы:
- `var(--ids__bg-surface)` — новый формат
- `rgba(var(--ids__surface-RGB), 0.5)` — старый формат
- `rgb(var(--ids__text-RGB))` — ещё один формат

**Где:**
- `QuickPick.astro` — старый формат
- `CategoryCards.astro` — старый формат
- `ModelCardSemantic.astro` — новый формат

**Решение:**
1. Унифицировать все компоненты на новый формат
2. Или создать алиасы для совместимости

---

### 🟡 DESIGN-007: Страница /story — демо IDS, не для прода

**Где:** `src/pages/story.astro`  
**Проблема:** Это демо-страница Intuition Design System, не имеет отношения к AIGirlsHub.

**Решение:**
1. Удалить страницу
2. Или переместить в `/ids-docs` (который тоже надо удалить)

---

### 🟡 DATA-005: Дублирование данных целей

**Проблема:** Цели (flirt, roleplay, companion...) определены в 3 местах:
1. `src/pages/index.astro` — goals array
2. `src/pages/for/[goal].astro` — goalsMeta object
3. `src/components/Header.astro` — goals array

**Влияние:** При добавлении новой цели нужно менять 3 файла.

**Решение:**
1. Создать `src/data/goals.ts` с единым источником
2. Импортировать везде

---

## 11. ОБНОВЛЁННЫЙ ПЛАН ИСПРАВЛЕНИЙ

### ФАЗА 0.5: КРИТИЧЕСКИЕ UX БАГИ (до основных фаз)

| # | Задача | Файлы | Приоритет |
|---|--------|-------|-----------|
| 0.5.1 | Убрать битую ссылку /top из Footer | `Footer.astro` | 🔴 |
| 0.5.2 | Убрать дублирующую кнопку "Подробнее" из карточек | `ModelCardSemantic.astro` | 🔴 |
| 0.5.3 | Ограничить карточки на /for/[goal] до 4 | `for/[goal].astro` | 🔴 |
| 0.5.4 | Убрать/скрыть секцию "Какой опыт ищешь?" | `for/[goal].astro` | 🟠 |
| 0.5.5 | Ограничить таблицу сравнения до 3 моделей | `for/[goal].astro` | 🟠 |

---

### ФАЗА 1.5: ИСПОЛЬЗОВАТЬ ГОТОВЫЕ КОМПОНЕНТЫ

| # | Задача | Файлы | Приоритет |
|---|--------|-------|-----------|
| 1.5.1 | Добавить QuickPick на главную | `index.astro` | 🟠 |
| 1.5.2 | Исправить CategoryCards (если нужен) | `CategoryCards.astro` | 🟡 |
| 1.5.3 | Добавить превью сервисов на главную | `index.astro` | 🟠 |

---

### ФАЗА 6.5: РЕФАКТОРИНГ ДАННЫХ

| # | Задача | Файлы | Приоритет |
|---|--------|-------|-----------|
| 6.5.1 | Создать единый источник целей | `src/data/goals.ts` | 🟡 |
| 6.5.2 | Унифицировать CSS переменные | Все компоненты | 🟢 |

---

## 12. КОНКРЕТНЫЕ ИСПРАВЛЕНИЯ (Дополнение)

### D.1 Исправить Footer.astro

**Файл:** `src/components/Footer.astro`

**Заменить строки 13-19:**
```astro
const links = [
  { href: '/catalog', label: 'Каталог' },
  { href: '/compare', label: 'Сравнения' },
  { href: '/about', label: 'О проекте' },
  { href: '/privacy', label: 'Политика' },
];
```

---

### D.2 Упростить ModelCardSemantic.astro

**Файл:** `src/components/ModelCardSemantic.astro`

**Заменить CTA секцию (строки 108-118):**
```astro
{showCTA && (
  <div class="semantic-card__cta">
    <a href={model.affiliateUrl} class="semantic-card__btn semantic-card__btn--primary" target="_blank" rel="noopener">
      Попробовать →
    </a>
  </div>
)}
```

---

### D.3 Ограничить карточки на /for/[goal].astro

**Файл:** `src/pages/for/[goal].astro`

**Заменить грид моделей:**
```astro
<!-- Топ-4 сервиса для этой цели -->
<Wrapper>
  <section class="goal-models">
    <h2>Лучшие сервисы для {meta.title.toLowerCase()}</h2>
    
    <div class="goal-models__grid">
      {models.slice(0, 4).map(model => (
        <ModelCardSemantic model={model} showCTA={true} />
      ))}
    </div>
    
    {models.length > 4 && (
      <div class="goal-models__more">
        <a href="/catalog">Показать все {models.length} сервисов →</a>
      </div>
    )}
  </section>
</Wrapper>
```

---

### D.4 Добавить QuickPick на главную

**Файл:** `src/pages/index.astro`

**Добавить после секции целей:**
```astro
import QuickPick from '../components/QuickPick.astro';
...
<Space size="L" />
<Wrapper>
  <QuickPick />
</Wrapper>
```

---

## 13. ЧЕКЛИСТ USER FLOW

### Путь 1: Главная → Цель → Модель → Affiliate
- [ ] Главная загружается без ошибок
- [ ] Карточки целей кликабельны
- [ ] Страница цели не перегружена (max 4 карточки)
- [ ] Клик по карточке модели → страница модели
- [ ] CTA "Попробовать" → affiliate URL

### Путь 2: Главная → Каталог → Фильтр → Модель
- [ ] Фильтры в каталоге работают
- [ ] Карточки ведут на страницы моделей
- [ ] Breadcrumbs корректны

### Путь 3: Главная → Сравнения → Сравнение → Модель
- [ ] Страница сравнений загружается
- [ ] Карточки сравнений кликабельны
- [ ] CTA к обеим моделям работают

### Путь 4: Footer навигация
- [ ] Все ссылки в footer работают
- [ ] Нет битых ссылок

---

*План обновлён: 2025-12-11*  
*Версия: 2.0 — Глубокий UX аудит*
