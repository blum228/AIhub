# UI ЛОГИЧЕСКИЙ АУДИТ

> Анализ консистентности интерфейса

---

## 🔴 КРИТИЧЕСКИЕ НЕСООТВЕТСТВИЯ

### 1. ФИЛЬТРЫ — ДВА РАЗНЫХ ПАТТЕРНА

**Проблема:**
- **Главная** → категории = горизонтальные pills БЕЗ "Все"
- **Каталог** → фильтры = горизонтальные pills С "Все" (active)
- **Страница тега** → НЕТ фильтров вообще

**Логическая ошибка:** Пользователь кликает на категорию на главной → попадает на `/tag/free` → не видит других категорий → тупик.

**Решение:** ЕДИНЫЙ паттерн фильтрации на ВСЕХ страницах:
- Главная: убрать категории ИЛИ сделать их идентичными каталогу
- Каталог: фильтры
- Страница тега: ТЕ ЖЕ фильтры с активным текущим тегом

---

### 2. ПОИСК — ФЕЙКОВЫЙ

**Проблема:** Иконка поиска в header ведёт на `/catalog`. Это обман пользователя.

**Решение:** Убрать иконку поиска. Если нет реального поиска — не показывать.

---

### 3. ДУБЛИРОВАНИЕ НАВИГАЦИИ

**Проблема:**
- Header: `Каталог | Топы | Сравнения`
- Главная: категории → ведут на `/tag/*`
- Главная: "Все сервисы" → ссылка "Каталог →"
- Hero: кнопка "Смотреть каталог →"

**4 способа попасть в каталог.** Это избыточно.

**Решение:** Одна точка входа в каталог — через header. Убрать дублирование.

---

### 4. BREADCRUMBS — НЕЛОГИЧНАЯ ИЕРАРХИЯ

**Текущая структура:**
```
/tag/free → Главная → Каталог → Бесплатные AI-девушки
```

**Проблема:** `/tag/free` НЕ является дочерней страницей `/catalog`. Это параллельные сущности.

**Решение:** 
```
/tag/free → Главная → Бесплатные
```
ИЛИ объединить tag и catalog в одну страницу с фильтрацией.

---

### 5. СТРАНИЦА ТЕГА — ИЗОЛИРОВАННАЯ

**Проблема:** На `/tag/free` нет способа переключиться на другую категорию без возврата в каталог.

**Решение:** Добавить те же фильтры, что и в каталоге.

---

## 🟡 СРЕДНИЕ НЕСООТВЕТСТВИЯ

### 6. ЗАГОЛОВКИ СТРАНИЦ — РАЗНЫЙ СТИЛЬ

- Главная: `h1` = "Найди свою AI-девушку" (призыв)
- Каталог: `h1` = "Каталог" (название)
- Тег: `h1` = "Бесплатные AI-девушки" (описание)

**Решение:** Унифицировать — везде название раздела.

### 7. СЧЁТЧИКИ — РАЗНЫЙ ФОРМАТ

- Главная категории: `Бесплатные 2`
- Каталог фильтры: `Все 10`
- Тег: `10 сервисов`

**Решение:** Единый формат: `Название (N)` или `Название • N`

---

## ПРАВИЛЬНАЯ АРХИТЕКТУРА

### Вариант A: Каталог = единая точка фильтрации

```
/              → Главная (hero + превью карточек)
/catalog       → Каталог со ВСЕМИ фильтрами
/catalog?f=free → Каталог с активным фильтром (JS или статика)
/models/[slug] → Страница модели
```

**Плюсы:** Один паттерн, нет дублирования
**Минусы:** Нужен JS для фильтрации или много статических страниц

### Вариант B: Теги = подстраницы каталога (текущий, но исправленный)

```
/              → Главная (hero + превью карточек, БЕЗ категорий)
/catalog       → Каталог (все + фильтры)
/catalog/free  → Каталог с фильтром "free" (те же фильтры, другой active)
/models/[slug] → Страница модели
```

**Плюсы:** Статика, SEO-friendly
**Минусы:** Нужно переименовать `/tag/` в `/catalog/`

### Вариант C: Минимализм (рекомендую)

```
/              → Главная (hero + ВСЕ карточки)
/models/[slug] → Страница модели
/top/[slug]    → Топы
/compare/[slug] → Сравнения
```

**Плюсы:** Максимально просто, нет путаницы
**Минусы:** Нет фильтрации (но при 10 моделях она не нужна!)

---

## ПЛАН ИСПРАВЛЕНИЙ

### Выбираю Вариант C — Минимализм

При 10 моделях фильтрация избыточна. Пользователь может просканировать все карточки за 5 секунд.

| # | Задача | Файлы |
|---|--------|-------|
| 1 | Убрать поиск из header | Header.astro |
| 2 | Убрать категории с главной | index.astro |
| 3 | Показать ВСЕ модели на главной | index.astro |
| 4 | Убрать ссылку "Каталог →" из секции | index.astro |
| 5 | Упростить header: только `Топы | Сравнения` | Header.astro |
| 6 | Удалить страницу каталога | catalog.astro |
| 7 | Удалить страницы тегов | tag/[tag].astro |
| 8 | Обновить footer | Footer.astro |

### Альтернатива: Вариант B — если нужны категории

| # | Задача | Файлы |
|---|--------|-------|
| 1 | Убрать поиск из header | Header.astro |
| 2 | Убрать категории с главной | index.astro |
| 3 | Переименовать `/tag/` в `/catalog/` | pages/ |
| 4 | Добавить фильтры на страницу тега | tag/[tag].astro |
| 5 | Унифицировать breadcrumbs | tag/[tag].astro |
| 6 | Унифицировать счётчики | везде |

---

## РЕКОМЕНДАЦИЯ

**Для 10 моделей — Вариант C (минимализм).**

Фильтрация нужна когда моделей 50+. При 10 — это overengineering.

Главная страница = каталог. Всё на одном экране.

---

*Аудит: 2025-12-11*
