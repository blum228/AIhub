# ФАЗА 2: ДИЗАЙН-КОНСИСТЕНТНОСТЬ

> **Приоритет:** 🟡 Средний  
> **Время:** ~4-6 часов  
> **Цель:** Унифицировать визуальный стиль

---

## Задачи

### 2.1 ✅ Унифицировать отступы между секциями
**Файлы:** Все страницы

**Стандарт:**
- Между секциями: `<Space size="XL" />`
- Внутри секции: `<Space size="L" />`
- Между элементами: `<Space size="M" />`

**Проверить:**
```bash
grep -r "Space size=" src/pages/
```

---

### 2.2 ✅ Унифицировать заголовки секций
**Файлы:** Все страницы

**Проблема:** Разные стили:
- `<h2>` с классом
- `<SectionTitle>`
- Просто `<h2>`

**Решение:** Использовать `<SectionTitle>` везде для заголовков секций.

```astro
import SectionTitle from '../components/SectionTitle.astro';
...
<SectionTitle>Популярные сервисы</SectionTitle>
```

---

### 2.3 ✅ Проверить мобильную адаптацию
**Файлы:** Все компоненты

**Проверить:**
- [ ] Header — мобильное меню работает
- [ ] Карточки — stack на мобильных
- [ ] Таблицы — горизонтальный скролл или альтернатива
- [ ] Footer — адаптирован
- [ ] Формы — touch-friendly

**Breakpoints:**
```css
/* Mobile: < 640px */
/* Tablet: 640px - 1024px */
/* Desktop: > 1024px */
```

---

### 2.4 ✅ Унифицировать стили карточек
**Файлы:** 
- `Card.astro`
- `ModelCardSemantic.astro`
- `ComparisonCard.astro`

**Проблема:** Разные стили карточек на разных страницах.

**Решение:** Базовый стиль через `Card.astro`, варианты через модификаторы:
- `.card` — базовый
- `.card--interactive` — с hover эффектами
- `.card--compare` — для сравнений

---

### 2.5 ✅ Унифицировать CSS переменные
**Файлы:** Все компоненты

**Проблема:** Разные форматы:
- `var(--ids__bg-surface)` — новый ✅
- `rgba(var(--ids__surface-RGB), 0.5)` — старый ❌
- `rgb(var(--ids__text-RGB))` — старый ❌

**Решение:** Перевести все на новый формат.

**Поиск старого формата:**
```bash
grep -r "ids__.*-RGB" src/components/
```

---

### 2.6 ✅ Решить вопрос с эмодзи/иконками
**Файлы:** Компоненты с иконками

**Текущее состояние:** Везде эмодзи 🔥 🎭 💬 🎤 📸

**Варианты:**
1. **Оставить эмодзи** — универсальны, не требуют загрузки
2. **Заменить на SVG** — Lucide, Heroicons
3. **Создать Icon.astro** — единый компонент

**Рекомендация:** Оставить эмодзи для MVP, заменить позже.

---

## Чеклист завершения

- [x] Отступы между секциями унифицированы (XL)
- [x] Заголовки секций используют SectionTitle
- [x] Мобильная версия работает корректно
- [x] Карточки имеют единый базовый стиль
- [x] CSS переменные в новом формате
- [x] Визуальная консистентность на всех страницах

---

*После завершения → переходи к PHASE-3-UX.md*
