# ФАЗА 6: БУДУЩИЕ УЛУЧШЕНИЯ (ОПЦИОНАЛЬНО)

> **Приоритет:** 🟢 Низкий  
> **Время:** По мере необходимости  
> **Цель:** Улучшения для масштабирования

---

## Задачи

### 6.1 🔮 Добавить поиск (Pagefind)
**Сложность:** Высокая

**Описание:** Статический поиск по сайту.

**Установка:**
```bash
npm install pagefind
```

**Настройка:**
```javascript
// astro.config.mjs
import { defineConfig } from 'astro/config';

export default defineConfig({
  build: {
    format: 'directory'
  }
});

// package.json
"scripts": {
  "postbuild": "pagefind --site dist"
}
```

**Альтернатива:** Fuse.js для клиентского fuzzy search.

---

### 6.2 🔮 Dark mode
**Сложность:** Высокая

**Описание:** Переключение светлой/тёмной темы.

**Шаги:**
1. Добавить CSS переменные для dark mode
2. Создать компонент ThemeToggle
3. Сохранять выбор в localStorage
4. Учитывать prefers-color-scheme

---

### 6.3 🔮 Автогенерация сравнений
**Сложность:** Средняя

**Описание:** Генерировать страницы сравнений автоматически на основе данных моделей.

**Подход:**
1. Создать функцию `generateComparison(model1, model2)`
2. Использовать данные из YAML для таблицы
3. Генерировать текст через шаблоны

---

### 6.4 🔮 Динамические OG-картинки
**Сложность:** Средняя

**Описание:** Генерировать OG-картинки автоматически.

**Инструменты:**
- Satori (от Vercel)
- @vercel/og
- Puppeteer (тяжёлый)

**Пример с Satori:**
```typescript
// src/pages/og/[slug].png.ts
import satori from 'satori';
import { html } from 'satori-html';

export async function GET({ params }) {
  const svg = await satori(
    html`<div style="...">...</div>`,
    { width: 1200, height: 630 }
  );
  // Convert to PNG
}
```

---

### 6.5 🔮 A/B тестирование CTA
**Сложность:** Высокая

**Описание:** Тестировать разные варианты CTA кнопок.

**Инструменты:**
- Google Optimize (deprecated)
- Posthog
- Custom solution с cookies

---

### 6.6 🔮 Единый источник данных целей
**Сложность:** Низкая

**Описание:** Вынести цели в отдельный файл.

**Создать `src/data/goals.ts`:**
```typescript
export const goals = [
  {
    slug: 'flirt',
    title: 'Флирт',
    emoji: '🔥',
    description: 'Лёгкое общение и флирт',
  },
  {
    slug: 'roleplay',
    title: 'Ролевые игры',
    emoji: '🎭',
    description: 'Сценарии и персонажи',
  },
  // ...
];

export type Goal = typeof goals[number];
```

**Использовать везде:**
```astro
import { goals } from '../data/goals';
```

---

### 6.7 🔮 Интернационализация (i18n)
**Сложность:** Высокая

**Описание:** Поддержка нескольких языков.

**Инструменты:**
- astro-i18next
- Paraglide
- Custom solution

---

### 6.8 🔮 RSS Feed
**Сложность:** Низкая

**Описание:** RSS для новых моделей/обзоров.

**Установка:**
```bash
npm install @astrojs/rss
```

---

### 6.9 🔮 Analytics
**Сложность:** Низкая

**Варианты:**
- Plausible (privacy-friendly)
- Umami (self-hosted)
- Google Analytics 4

---

### 6.10 🔮 Performance оптимизация
**Сложность:** Средняя

**Шаги:**
1. Lazy loading изображений
2. Prefetch для навигации
3. Минификация CSS/JS
4. CDN для статики

---

## Приоритизация

| Задача | Влияние | Сложность | Рекомендация |
|--------|---------|-----------|--------------|
| Поиск | Высокое | Высокая | При 20+ моделях |
| Dark mode | Среднее | Высокая | Не срочно |
| Автогенерация сравнений | Высокое | Средняя | При 15+ моделях |
| OG-картинки | Среднее | Средняя | Для SEO |
| Единый источник целей | Низкое | Низкая | Сделать сейчас |
| RSS | Низкое | Низкая | Опционально |
| Analytics | Высокое | Низкая | Сделать сейчас |

---

*Это опциональная фаза. Выполнять по мере роста проекта.*
