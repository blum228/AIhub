# ФАЗА 3: UX УЛУЧШЕНИЯ

> **Приоритет:** 🟡 Средний  
> **Время:** ~6-8 часов  
> **Цель:** Улучшить пользовательский опыт и конверсию

---

## Задачи

### 3.1 ✅ Добавить QuickPick на главную
**Файл:** `src/pages/index.astro`

**Проблема:** Компонент QuickPick существует, но не используется.

**Решение:**
```astro
import QuickPick from '../components/QuickPick.astro';
...
<Space size="L" />
<Wrapper>
  <QuickPick />
</Wrapper>
```

**Компонент показывает:**
- Бесплатно → Character.AI
- Генерация фото → Candy.AI
- На русском → Replika

---

### 3.2 ✅ Добавить превью сервисов на главную
**Файл:** `src/pages/index.astro`

**Проблема:** Пользователь не видит что за сервисы его ждут.

**Решение:** Добавить секцию "Популярные сервисы" с 3 логотипами/названиями.

```astro
<Space size="L" />
<Wrapper>
  <section class="featured-services">
    <p class="featured-services__text">
      Candy.AI • Replika • Character.AI и ещё 7 сервисов
    </p>
  </section>
</Wrapper>
```

---

### 3.3 ✅ Раскрыть первые FAQ по умолчанию
**Файл:** `src/pages/index.astro` или `FAQ.astro`

**Проблема:** FAQ свёрнут — SEO-контент скрыт.

**Решение:** Раскрыть первые 2 вопроса по умолчанию.

```astro
{faqItems.map((item, index) => (
  <details open={index < 2}>
    <summary>{item.question}</summary>
    <p>{item.answer}</p>
  </details>
))}
```

---

### 3.4 ✅ Добавить галерею на страницу модели
**Файл:** `src/pages/models/[slug].astro`

**Проблема:** Пользователь не видит интерфейс сервиса.

**Решение:**
1. Добавить поле `screenshots: string[]` в схему модели
2. Использовать компонент `Gallery.astro` (уже есть в IDS)
3. Добавить секцию "Скриншоты" на страницу

```astro
{model.screenshots && model.screenshots.length > 0 && (
  <section class="model-screenshots">
    <SectionTitle>Скриншоты</SectionTitle>
    <Gallery images={model.screenshots} />
  </section>
)}
```

---

### 3.5 ✅ Добавить индикацию свежести контента
**Файлы:** Карточки, страницы моделей

**Проблема:** Пользователь не знает, актуальны ли обзоры.

**Решение:**
1. Добавить поле `updatedAt: Date` в схему
2. Показывать "Обновлено: декабрь 2025"

```astro
<span class="updated-badge">
  Обновлено: {new Date(model.updatedAt).toLocaleDateString('ru-RU', { month: 'long', year: 'numeric' })}
</span>
```

---

### 3.6 ✅ Добавить навигацию "назад к цели"
**Файл:** `src/pages/models/[slug].astro`

**Проблема:** Breadcrumbs: Главная → Каталог → [Модель], но если пришёл с `/for/flirt` — теряется контекст.

**Решение:** Добавить блок "Другие сервисы для [цели]" внизу страницы.

```astro
{model.goals && model.goals.length > 0 && (
  <section class="related-models">
    <SectionTitle>Другие сервисы для {model.goals[0]}</SectionTitle>
    <!-- Показать 3 похожих модели -->
  </section>
)}
```

---

### 3.7 ✅ Улучшить страницу сравнений
**Файл:** `src/pages/compare/index.astro`

**Проблема:** Скучный список из 3 сравнений.

**Решение:**
- Добавить заголовок "Популярные сравнения"
- Добавить CTA "Не нашёл нужное сравнение? → Каталог"

---

## Чеклист завершения

- [x] QuickPick добавлен на главную
- [x] Превью сервисов на главной
- [x] Первые 2 FAQ раскрыты
- [x] Галерея на странице модели (если есть скриншоты)
- [x] Индикация свежести контента
- [x] Навигация "назад к цели" работает
- [x] Страница сравнений улучшена

---

*После завершения → переходи к PHASE-4-CONTENT.md*
