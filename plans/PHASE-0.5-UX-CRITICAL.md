# ФАЗА 0.5: КРИТИЧЕСКИЕ UX БАГИ

> **Приоритет:** 🔴 Высокий  
> **Время:** ~3-4 часа  
> **Цель:** Устранить UX-проблемы, мешающие конверсии

---

## Задачи

### 0.5.1 ✅ Убрать дублирующую кнопку "Подробнее"
**Файл:** `src/components/ModelCardSemantic.astro`

**Проблема:** Карточка имеет:
- Клик по карточке → `/models/[slug]`
- Кнопка "Подробнее" → `/models/[slug]` (дубль!)
- Кнопка "Попробовать →" → affiliate URL

**Решение:** Убрать кнопку "Подробнее", оставить только "Попробовать →".

```astro
{showCTA && (
  <div class="semantic-card__cta">
    <a href={model.affiliateUrl} class="semantic-card__btn semantic-card__btn--primary" target="_blank" rel="noopener">
      Попробовать →
    </a>
  </div>
)}
```

---

### 0.5.2 ✅ Ограничить карточки на /for/[goal] до 4
**Файл:** `src/pages/for/[goal].astro`

**Проблема:** Слишком много карточек с CTA → cognitive overload.

**Решение:** Показывать топ-4, остальные — ссылка "Показать все".

```astro
<div class="goal-models__grid">
  {models.slice(0, 4).map(model => (
    <ModelCardSemantic model={model} showCTA={true} />
  ))}
</div>

{models.length > 4 && (
  <div class="goal-models__more">
    <a href="/catalog">Показать все {models.length} сервисов →</a>
  </div>
)}
```

---

### 0.5.3 ✅ Убрать секцию "Какой опыт ищешь?"
**Файл:** `src/pages/for/[goal].astro`

**Проблема:** Карточки типов опыта не кликабельны, не фильтруют — мёртвый UI.

**Решение:** Удалить секцию полностью.

---

### 0.5.4 ✅ Ограничить таблицу сравнения до 3 моделей
**Файл:** `src/pages/for/[goal].astro`

**Проблема:** Таблица с 8+ колонками — UX катастрофа на мобильных.

**Решение:** Показывать только топ-3 модели в таблице.

```astro
<FeatureComparisonTable 
  models={models.slice(0, 3)} 
  goal={goal} 
/>
```

Добавить ссылку "Сравнить все →" к `/compare`.

---

### 0.5.5 ✅ Исправить CategoryCards (если используется)
**Файл:** `src/components/CategoryCards.astro`

**Проблема:** Ссылки `/tag/${cat.slug}` не существуют.

**Решение:** Заменить на `/catalog/${cat.slug}` или удалить компонент.

---

## Чеклист завершения

- [x] Карточки имеют только один CTA "Попробовать"
- [x] На `/for/[goal]` показывается max 4 карточки
- [x] Секция "Какой опыт ищешь?" удалена
- [x] Таблица сравнения показывает max 3 модели
- [x] CategoryCards удалён (не использовался)
- [x] Страницы не перегружены информацией

---

*После завершения → переходи к PHASE-1-ARCHITECTURE.md*
