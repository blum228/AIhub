# ФАЗА 0: КРИТИЧЕСКИЕ БАГИ

> **Приоритет:** 🔴 Максимальный  
> **Время:** ~2-3 часа  
> **Цель:** Устранить блокирующие проблемы

---

## Задачи

### 0.1 ✅ Фильтры в каталоге
**Файл:** `src/pages/catalog.astro`

**Проблема:** Кнопки фильтров без функционала, хотя компонент `CatalogFilters` существует.

**Решение:**
```astro
// Заменить кнопки на компонент
import CatalogFilters from '../components/CatalogFilters.astro';
...
<CatalogFilters activeFilter="all" totalCount={models.length} />
```

---

### 0.2 ✅ Битая ссылка в Footer
**Файл:** `src/components/Footer.astro`

**Проблема:** Ссылка `/top` не существует.

**Решение:** Удалить ссылку на `/top` из массива links.

---

### 0.3 ✅ Битая ссылка в Hero.astro
**Файл:** `src/components/Hero.astro` строка 32

**Проблема:** Ссылка `/top/best-free` не существует.

**Решение:** Заменить на `/catalog/free`.

---

### 0.4 ✅ Удалить страницу ids-docs
**Файл:** `src/pages/ids-docs.astro`

**Проблема:** Документация IDS не должна быть на проде.

**Решение:** Удалить файл.

---

### 0.5 ✅ Проверить страницу story
**Файл:** `src/pages/story.astro`

**Проблема:** Демо-страница IDS, не для прода.

**Решение:** Удалить файл.

---

## Команды для проверки

```bash
# Поиск битых ссылок на /top/
grep -r "/top/" src/

# Проверка сборки
npm run build
```

---

## Чеклист завершения

- [x] Фильтры в `/catalog` работают
- [x] Footer не содержит битых ссылок
- [x] Hero.astro не содержит битых ссылок
- [x] Страница `/ids-docs` удалена
- [x] Страница `/story` удалена
- [x] `npm run build` проходит без ошибок

---

*После завершения → переходи к PHASE-0.5-UX-CRITICAL.md*
